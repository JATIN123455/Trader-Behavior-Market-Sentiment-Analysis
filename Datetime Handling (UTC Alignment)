# Convert timestamps
trades_df["timestamp"] = pd.to_datetime(trades_df["timestamp"], utc=True)
sentiment_df["date"] = pd.to_datetime(sentiment_df["date"])

# Create merge keys
trades_df["trade_date_utc"] = trades_df["timestamp"].dt.date
sentiment_df["sentiment_date"] = sentiment_df["date"].dt.date
